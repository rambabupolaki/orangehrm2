package selenium.framework.pages;

import org.openqa.selenium.WebDriver;

import selenium.framework.common.PageControls;

public class DashboardPage extends PageControls{
	/**
	 *  Description:This is Dashboard page constructor. It holds all the element locators in Dashboard page
	 * @param webDriver
	 */
	public DashboardPage(WebDriver driver) {
		super(driver);
		
	}
}
