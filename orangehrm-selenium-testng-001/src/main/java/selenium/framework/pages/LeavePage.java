package selenium.framework.pages;

import org.openqa.selenium.WebDriver;

import selenium.framework.common.PageControls;

public class LeavePage extends PageControls{
	/**
	 *  Description:This is Leave page constructor. It holds all the element locators in Leave page
	 * @param webDriver
	 */
	public LeavePage(WebDriver driver) {
		super(driver);
		}
}
